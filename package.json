{
  "name": "start",
  "private": true,
  "version": "0.1.0-alpha.1",
  "license": "MIT",
  "description": "your coding agent",
  "type": "module",
  "packageManager": "pnpm@10.25.0",
  "homepage": "https://start.intelligence.one",
  "scripts": {
    "format:fix": "biome format . --write",
    "lint": "biome lint . --error-on-warnings",
    "check:pins": "node scripts/check-dependency-pins.js",
    "hooks:install": "git config core.hooksPath .githooks",
    "dev": "pnpm --filter @intelligence-one/start-desktop dev",
    "audit": "pnpm run audit:deps && pnpm run audit:signatures",
    "dist": "pnpm --filter @intelligence-one/start-desktop dist",
    "build": "pnpm --filter @intelligence-one/start-desktop build",
    "audit:deps": "pnpm audit && pnpm --dir packages/desktop audit",
    "package": "pnpm --filter @intelligence-one/start-desktop package",
    "format": "biome ci --linter-enabled=false --assist-enabled=false .",
    "typecheck": "pnpm --filter @intelligence-one/start-desktop typecheck",
    "check:fix": "pnpm run check:pins && biome check . --write && pnpm run typecheck",
    "check": "pnpm run check:pins && pnpm run lint && pnpm run format && pnpm run typecheck",
    "audit:signatures": "npm audit signatures && npm --prefix packages/desktop audit signatures"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "electron"
    ],
    "patchedDependencies": {
      "@earendil-works/pi-ai@0.75.4": "patches/@earendil-works__pi-ai@0.75.4.patch"
    }
  }
}
